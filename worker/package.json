{
  "name": "bush-riding-map-api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute bush-riding-map --remote --file schema.sql",
    "db:init:local": "wrangler d1 execute bush-riding-map --local --file schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.105.0"
  }
}
