# docs/ holds the archived Claude Design handoff bundle — HTML/CSS prototypes
# and reference material, not theme code. Shopify never uploads it and Theme
# Check shouldn't read it either.
ignore:
  - docs/**

MatchingTranslations:
  enabled: false
TemplateLength:
  enabled: false
# lighthouse ci retest trigger 3
