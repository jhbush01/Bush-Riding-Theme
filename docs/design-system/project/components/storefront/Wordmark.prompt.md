The logo. It is an image, never a font — do not try to typeset it. Size by height.

```jsx
<Wordmark href="/" height={62} />
```

Steps down to ~38px on mobile. Keep `shadow` on when it sits over photography.
