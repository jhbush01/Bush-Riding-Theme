The header's text links. They sit over full-bleed media, so they are always translucent with a soft shadow — never a solid colour on a solid bar.

```jsx
<NavLink tone="home" href="/collections/all">Shop</NavLink>
<NavLink tone="home" clock>9:14 AEST</NavLink>
```
