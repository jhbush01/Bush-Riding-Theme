Bush Map's button. Wrap the app in a `.brm` element so the map tokens resolve.

```jsx
<div className="brm"><Button variant="primary">Get the route</Button></div>
```

primary = the dark full-width CTA in the email gate and card; danger = destructive (delete a ride).
