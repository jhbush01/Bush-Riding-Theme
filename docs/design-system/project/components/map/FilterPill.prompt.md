Toggles a pin category on the map. The dot matches the pin colour (routes olive, bush terracotta, famous plum).

```jsx
<FilterPill tone="famous" active={show} onClick={toggle}>Famous Events</FilterPill>
```
